# iOS 0.3.0 — deployment gate

Candidate validation: PASS.

Promoted to `main` only after automated validation of:
- frozen iOS 0.1 visual core hashes;
- JavaScript syntax;
- native Gemini Live WebSocket client contract;
- PCM16 16 kHz input and 24 kHz output paths;
- automatic VAD and barge-in;
- absence of Gemini API keys, Owner capability, Qwen, local TTS, and `speechSynthesis` from public Mobile files;
- secure Supabase token broker health and unauthorized-device rejection.

Physical iPhone validation remains required before freezing 0.3.
