name: Diagnose Live Token

on:
  push:
    branches: [diagnostic/live-token-probe]
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe full Gemini Live setup
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS 'https://novgwydgcvlboujnmygq.supabase.co/functions/v1/loky-pc4-mobile-token?liveprobe=1'
